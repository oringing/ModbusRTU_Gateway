# Modbus RTU 网关（STM32F103C8T6）

> **版本**: v1.7.0 | **许可证**: MIT | **最后更新**: 2026-05-15  
> 基于 STM32F1 + FreeRTOS 的工业级 Modbus RTU 协议从机网关，具备高稳定性 UART 接收链、故障自恢复机制、双路异构舵机控制与系统心跳监控。

## 🔍 技术亮点

### ✅  UART 接收链防护

- **背靠背请求帧丢失防护**：通过空闲中断（IDLE）+ 双缓冲 + HAL 状态机同步重绑，消除奇偶交替响应故障
- **错误分级恢复机制**：ORE 立即恢复、FE/NE 连续 3 次触发恢复、PE 连续 5 次触发恢复，避免误判导致频繁重启
- **硬件故障熔断保护**：恢复重试超限（10 次）后标记硬件故障，进入系统降级运行，连续 5 帧正常后自动恢复

### ✅ 故障预警与自恢复

- **独立看门狗（IWDG）**：2 秒超时复位（LSI 40kHz / 预分频 256 / 重载值 312），修正 LSI 时钟开启与重载值计算陷阱
- **系统心跳监控**：寄存器 `0x0003` Bit 0~3 每秒翻转（`0x000F ↔ 0x0000`），提供 MCU 存活及调度正常的直观证据
- **栈水位监控**：周期性检测任务栈高水位（60s 间隔），低水位告警与恢复通知机制，预防栈溢出风险
- **安全模式停机**：配置校验 fail-fast，LED 编码指示 HardFault/MemManage/BusFault 等严重故障

### ✅ 协议层防护

- **协议层入参校验**：在 `Modbus_Process()` 入口处校验帧长度、从机地址、CRC，非法帧直接丢弃
- **寄存器访问边界保护**：读/写请求中校验起始地址与数量组合，防止数组越界和整数溢出
- **寄存器互斥保护**：单次加锁快照读取，写回调锁外执行，避免并发一致性风险
- **日志限流机制**：错误日志按频率控制输出，避免阻塞主流程

### ✅ 双路异构舵机控制

- **180° 舵机（SG90）**：地址 `0x0004`，通过 Modbus 写寄存器实现 0°~180° 精确角度控制（TIM3_CH1/PA6，50Hz PWM）
- **360° 连续旋转舵机**：地址 `0x0005`，通过 Modbus 写寄存器实现速度/方向控制（TIM3_CH2/PA7）
  - `0~126`：反转调速（值越小反转越快）
  - `127`：中位停止（死区）
  - `128~255`：正转调速（值越大正转越快）
- **脉宽级精确控制**：BSP 层提供微秒级脉宽控制接口，消除占空比切换毛刺影响

## ️ 技术栈

- **MCU**: STM32F103C8T6 (ARM Cortex-M3, 72MHz, 64KB Flash, 20KB RAM)
- **RTOS**: FreeRTOS v10.3.1 (CMSIS-RTOS v1 接口)
- **通信协议**: Modbus RTU over RS485 (MAX485, 自动流向控制型)
- **固件库**: STM32 HAL (F1)
- **工具链**: STM32CubeMX + Keil MDK v5 + VS Code
- **代码规范**: Doxygen 注释 + .clang-format 格式化

## 📊 性能指标

| 指标             | 数值                                 |
| -------------- | ---------------------------------- |
| 支持波特率          | 115200 bps                         |
| 最大帧长度          | 256 字节                             |
| 平均响应延迟         | < 1ms（典型值）                         |
| 看门狗超时          | 2 秒（LSI 40kHz / 预分频 256 / 重载值 312） |
| 内存占用           | \~10KB RAM (FreeRTOS heap)         |

## 📁 项目结构（部分）

```
ModbusRTU_Gateway/
├── Core/                 # 启动、时钟、中断、main
├── Bsp/                  # 硬件抽象层（UART/LED/KEY/EXTI）
├── App/                  # 应用层（Modbus 从机）
│   ├── Driver/           # OS 相关驱动封装（UART 互斥发送）
│   ├── Protocol/         # Modbus 协议处理（0x03/0x06）
│   ├── System/           # 系统控制、错误处理、配置校验
│   └── Task/             # LED/UART/Monitor 任务
├── docs/                 # 技术文档与记录
├── Drivers/              # HAL 库文件
├── MDK-ARM/              # Keil 工程文件
├── Middlewares/          # FreeRTOS 内核文件
└── Release/              # 当前版本预编译hex文件
```

***

## 🔌 硬件接线与图片

### 1. 系统整体与主从硬件

![系统整体图](docs/images/整体图V1.0.jpg)
![MAX485从机端图解](docs/images/max485从机端图解.jpg)
![MAX485主机硬件图](docs/images/max485主机硬件图.jpg)

### 2. TTL 转 RS485 模块（自动流向控制型）接线说明

| 模块引脚   | 连接 STM32或USB-TTL  | 说明                |
| ------ | ----------------- | ----------------- |
| VCC    | 3.3V              | 模块电源（3.3V/5V兼容）   |
| GND    | GND               | 共地                |
| TXD    | PA9 (USART1\_TX)  | 模块发送接MCU发送        |
| RXD    | PA10 (USART1\_RX) | 模块接收接MCU接收        |
| A      | A+ (主机侧)          | 485总线正极           |
| B      | B- (主机侧)          | 485总线负极           |
| 大地/屏蔽地 | -                 | 接大地端子，用于屏蔽层接地，抗干扰 |

```
STM32F103从机     MAX485模块（从机端）    MAX485模块（主机端）         PC USB转TTL    
┌─────────┐        ┌──────────┐           ┌──────────┐             ┌──────────┐ 
│ PA9  TX ├───────►│ TXD   A+ │◄─────────►│ A+   TXD │◄────────────│ TXD      │
│ PA10 RX │◄───────│ RXD      │           │      RXD │────────────►│ RXD      │
│ 3.3V    ├────────│ VCC   B- │◄─────────►│ B-   VCC │ 无隔离器不接  │ VCC      │
│ GND     ├────────│ GND      │           │      GND │─────────────│ GND      │
└─────────┘        └─────────┬┘           └┬─────────┘             └──────────┘
                           屏蔽地（悬空） 屏蔽地（悬空）

```

- 本模块为自动流向控制型，无需MCU控制RE/DE引脚，操作与普通串口一致。
- MCU的UART串口 或 PC的USB转TTL串口 的 TX 连接 本模块串口侧 的 TXD 引脚，RX同理，否则无法正常通信。

![TTL转RS485模块外形与接线](docs/images/TTL转RS485模块外形与接线.jpg)

### 3. 关键接线要点（务必检查）

- **USART1**：PA9 (TX)、PA10 (RX)
- **RS485**：A/B 极性必须与主机一致
- **电源地**：主从设备与 USB 转串口必须共地
- **抗干扰**：建议增加板级外部上拉/终端电阻，`GPIO_PULLUP` 仅作辅助

***

## ⚙️ 编译与烧录

### 开发环境要求

- **IDE**: Keil MDK v5（需安装 STM32F1 系列支持包）
- **调试器**: ST-LINK V2
- **固件库**: STM32 HAL (F1)
- **可选工具**: VS Code（用于文档编辑与代码浏览）

### 编译步骤

1. 打开工程：`MDK-ARM/ModbusRTU_Gateway.uvprojx`
2. 选择目标并编译（确保无警告和错误）
3. 连接 ST-LINK V2，下载并调试

### 烧录验证

- 可直接使用 [当前版本预编译 hex 文件](Release/ModbusRTU_Gateway.hex) 烧录验证功能
- 或通过编译生成的 hex 文件进行烧录

### 注意事项

- 若编译报错，检查是否已安装 STM32F1 系列 Device Family Pack
- 确保 `docs/images/` 下存在对应图片文件以正常显示文档

***

### 核心模块说明

- **Core/**: MCU 启动入口、时钟配置、中断向量表、FreeRTOS 初始化（`main.c`, `stm32f1xx_it.c`, `freertos.c`）
- **Bsp/**: 硬件抽象层，封装 GPIO、UART、按键、EXTI、舵机 PWM 等底层驱动（`uart.c`, `led.c`, `servo.c`）
- **App/Driver/**: 操作系统相关驱动封装（UART 互斥发送 `driver_uart.c`、舵机控制 `driver_servo.c`）
- **App/Protocol/**: Modbus RTU 协议解析与寄存器访问逻辑（`modbus.c`, 支持 0x03/0x06 功能码）
- **App/System/**: 系统控制、错误处理、配置校验、看门狗管理（`system_ctrl.c`, `error_handler.c`）
- **App/Task/**: FreeRTOS 任务实现（LED 心跳灯、UART 轮询处理、Monitor 栈监控与喂狗）
- **App/Test/**: 测试验证模块（`test_validation.c`，用于协议层功能测试）

***

## ️ 寄存器映射

### 保持寄存器地址表（10 个）

| 地址 | 宏定义 | 功能说明 | 读写 | 范围 | 联动功能 |
|------|--------|----------|------|------|----------|
| 0x0000 | `MODBUS_REG_ADDR_SENSOR_TEMP` | AHT20 温度（预留） | R | 0x1234 | 待接入 AHT20 |
| 0x0001 | `MODBUS_REG_ADDR_SENSOR_HUMIDITY` | AHT20 湿度（预留） | R | 0x5678 | 待接入 AHT20 |
| 0x0002 | `MODBUS_REG_ADDR_SENSOR_PRESSURE` | BMP280 气压（预留） | R | 0x2710 | 待接入 BMP280 |
| 0x0003 | `MODBUS_REG_ADDR_DEVICE_STATUS` | **系统状态寄存器** | R | 位定义见下 | 心跳+故障标志 |
| 0x0004 | `MODBUS_REG_ADDR_SERVO_TARGET` | **180° 舵机目标角度** | **W** | 0~180 | TIM3_CH1/PA6 |
| 0x0005 | `MODBUS_REG_ADDR_SERVO_SPEED` | **360° 舵机速度/方向** | **W** | 0~255 | TIM3_CH2/PA7 |
| 0x0006~0x0009 | - | 预留扩展 | R | 0x0000 | 未来功能扩展 |

**0x0003 系统状态寄存器位定义**：
- **Bit 0~3**: 心跳计数器（0x000F ↔ 0x0000，每秒翻转）
- **Bit 4**: AHT20 传感器就绪标志（0=未接入，1=就绪）
- **Bit 5**: BMP280 传感器就绪标志（0=未接入，1=就绪）
- **Bit 6**: 舵机驱动就绪标志（0=未初始化，1=就绪）
- **Bit 7**: UART 通信正常标志（0=异常，1=正常）
- **Bit 8~15**: 保留（未来扩展）

**舵机速度控制说明**：
- `0~126`：反转调速（0 = 最快反转，126 = 最慢反转）
- `127`：中位停止（死区，舵机静止）
- `128~255`：正转调速（128 = 最慢正转，255 = 最快正转）

***

## 🚀 启动流程

1. `main()` 完成 `HAL_Init()` 与时钟初始化（72MHz）
2. 初始化 BSP 与协议：`BSP_LED_Init()`、`BSP_UART_Init()`、`Modbus_Init()`
3. `System_Ctrl_Init()` 做配置校验，失败直接 `Error_Handler()` 并记录日志
4. `System_StartTasks()` 创建 LED/UART/Monitor 任务
5. `osKernelStart()` 启动 FreeRTOS 调度器

**任务启动顺序**：
- **Monitor_Task** 优先启动，负责看门狗喂狗（500ms 间隔）与系统心跳维护
- **UART_Task** 次之，处理 Modbus 协议解析与响应
- **LED_Task** 最后启动，提供系统运行状态指示

***

## 📋 任务与配置

### 当前运行任务

| 任务名 | 优先级 | 栈大小 | 实测峰值 | 功能说明 |
|--------|--------|--------|----------|----------|
| **UART_Task** | Normal | 125 words | 54 words (100ms/帧高压) | 周期调用 `Modbus_Process()`，处理 Modbus 请求 |
| **Monitor_Task** | BelowNormal | 165 words | 74 words (空闲/高压一致) | 心跳翻转、喂狗、栈水位监控 |
| **LED_Task** | Low | 64 words | 28 words (空闲/高压一致) | 500ms 翻转 LED，系统运行状态指示 |

**栈空间裕度**：所有任务栈预留 ≥1.3 倍剩余量，符合工业级安全标准

### 主要配置参数

配置文件：[App/System/Inc/system_config.h](App/System/Inc/system_config.h)

| 参数 | 值 | 说明 |
|------|-----|------|
| `LED_TASK_STACK_SIZE` | 64 words | 实测峰值 28 words，预留 1.3 倍剩余量 |
| `UART_TASK_STACK_SIZE` | 125 words | 实测峰值 54 words（100ms/帧高压），预留 1.3 倍剩余量 |
| `MONITOR_TASK_STACK_SIZE` | 165 words | 实测峰值 74 words，预留 1.3 倍剩余量 |
| `STACK_CHECK_INTERVAL_SEC` | 60 | 栈水位检查间隔（单位：秒） |
| `HEARTBEAT_INTERVAL_MS` | 1000 | 心跳翻转间隔（单位：ms） |
| `IWDG_FEED_INTERVAL_MS` | 500 | 看门狗喂狗间隔（单位：ms） |
| `MONITOR_TASK_BASE_DELAY` | 10 | 监控任务基础循环周期（单位：ms） |
| `SYSTEM_UART_TEXT_LOG_ENABLE` | 1 | 开启 USART1 ASCII 日志（用于调试阶段） |
| `SYSTEM_TASK_STOP_TIMEOUT_MS` | 300 | 任务退出等待超时（单位：ms） |

***

## 📄 技术文档索引

### 核心文档

- [docs/问题记录.md](docs/问题记录.md)：当前已知问题清单与改进计划
- [docs/日志记录.md](docs/日志记录.md)：开发里程碑记录与技术决策


***

## 🧪 快速测试

### 测试环境

- **串口助手**：SSCOM 等支持多字符串循环发送的串口工具
- **通信参数**：HEX 模式，波特率 115200，8N1，无校验
- **从机地址**：`1`
- **寄存器映射**：10 个保持寄存器（地址 `0x0000~0x0002` 为传感器数据，`0x0003` 为系统状态，`0x0004` 为 180° 舵机目标角度，`0x0005` 为 360° 舵机速度控制）

### 测试方法：多字符串循环发送

使用串口助手的**多字符串循环发送**功能，按顺序依次发送以下指令（建议设置最后一条指令延时 3000ms，便于观察串口日志输出）：

| 序号 | 发送指令（HEX） | 功能说明 | 延时 (ms) | 预期响应 |
|------|----------------|----------|-----------|----------|
| 1 | `01 06 00 04 00 00 B4` | 写地址 4，角度 0° | 100 | 原样回显 |
| 2 | `01 06 00 04 00 5A 8A 8C` | 写地址 4，角度 90° | 300 | 原样回显 |
| 3 | `01 06 00 04 00 B4 75 C4` | 写地址 4，角度 180° | 400 | 原样回显 |
| 4 | `01 06 00 05 00 7F 38 C3` | 写地址 5，速度 127（停止） | 300 | 原样回显 |
| 5 | `01 06 00 05 00 FF 78 03` | 写地址 5，速度 255（正转最大） | 300 | 原样回显 |
| 6 | `01 06 00 05 00 01 99 C3` | 写地址 5，速度 1（反转最大） | 300 | 原样回显 |
| 7 | `01 03 00 04 00 01 C5 CB` | 读地址 4（验证写入值） | 100 | 返回当前角度值 |
| 8 | `01 03 00 05 00 01 94 0B` | 读地址 5（验证写入值） | 100 | 返回当前速度值 |
| 9 | `01 03 00 00 00 03 C4 3A` | 读地址 0-2（传感器数据） | 100 | 返回传感器寄存器 |
| 10 | `01 03 00 00 00 03 C4 37` | **非法 CRC 测试帧** | 3000 | **无响应（直接丢弃）** |

**操作步骤**：
1. 在串口助手中切换到"多字符串发送"模式
2. 依次添加上述 10 条指令，并设置对应延时
3. 勾选"循环发送"，点击"发送"开始测试
4. 观察串口接收区：前 9 条指令应正常响应，第 10 条非法帧应无任何响应

### 单指令手动测试

如需单独测试某条指令，可直接在 HEX 发送框输入：

**读保持寄存器（0x03）**
```
发送: 01 03 00 00 00 03 C4 3A
响应: 返回寄存器地址 0x00、0x01 和 0x02 中的值（温度/湿度/气压）
```

**写单寄存器（0x06）**
```
发送: 01 06 00 04 00 5A 8A 8C  （写入地址 4，值 90）
响应: 01 06 00 04 00 5A 8A 8C  （原样回显）
验证: 01 03 00 04 00 01 C5 CB  （读取确认）
```

**舵机控制测试**
```
写 180° 舵机（地址 4）:
01 06 00 04 00 00 B4 6D  （0°）
01 06 00 04 00 5A 8A 8C  （90°）
01 06 00 04 00 B4 75 C4  （180°）

写 360° 舵机（地址 5）:
01 06 00 05 00 01 99 C3  （反转最大）
01 06 00 05 00 7F 38 C3  （停止）
01 06 00 05 00 FF 78 03  （正转最大）
```

**读系统状态（地址 3）**
```
发送: 01 03 00 03 00 01 54 0A
响应: 01 03 02 00 0F XX XX  （心跳 Bit 0-3 翻转，Bit 4-7 为设备标志）
```

### 异常情况测试

- **CRC 错帧**：无任何响应（直接丢弃，如测试表第 10 条）
- **越界地址**：返回 `01 86 02 XX XX`（异常码 0x02）
- **非法值**：返回 `01 86 03 XX XX`（异常码 0x03）
- **只读寄存器写**：返回 `01 86 03 XX XX`（异常码 0x03）

***

## 🔍 调试与排障要点

### 错误日志格式

```
ERR[类型] 文件:行号
```

类型编码：

- `ERR[1]`: ERROR\_HAL（HAL 调用或底层初始化错误）
- `ERR[2]`: ERROR\_UART（UART 接收链 / 恢复链异常）
- `ERR[4]`: ERROR\_STACK\_OVERFLOW（FreeRTOS 栈溢出）
- `ERR[5]`: ERROR\_SYSTEM（系统控制层错误）
- `ERR[6-9]`: 硬件故障（HardFault/MemManage/BusFault/UsageFault）

### 常见问题排查

1. **LED 常亮且无任务输出**：优先检查是否进入 `System_EnterSafeMode()`
2. **ERR\[2] 高频刷屏**：检查 UART 恢复路径是否误判 `HAL_BUSY`
3. **Modbus 无响应**：
   - 确认 HEX 模式、关闭自动换行
   - 检查主从波特率一致
   - 验证 CRC 校验码正确性
4. **图片不显示**：确认文件已放入 `docs/images/` 且文件名与文档中一致

***

## ⚠️ 已知约束与局限性

### 功能限制

- 当前仅实现 `0x03`（读保持寄存器）和 `0x06`（写单寄存器）功能码
- 寄存器总数：10 个，地址 `0x0004`（舵机角度）和 `0x0005`（舵机速度）可写，地址 `0x0000~0x0003` 为只读传感器/系统数据
- 未实现写多个寄存器（0x10）、线圈/离散输入等功能码
- 仅支持单字节 Modbus 从机地址（当前固定为 0x01）

### 测试局限性

- **UART 硬件错误触发**：实验室环境无法可靠触发 ORE/FE/NE 硬件错误标志位，真实工业干扰场景需进一步验证
- **长稳测试**：建议进行 24h+ 连续运行测试，验证任务栈稳定性与内存泄漏情况
- **舵机控制测试**：当前已验证 180° 舵机角度控制和 360° 舵机速度控制功能，但长期机械磨损影响未评估

### 硬件建议

- 工业现场建议增加板级外部上拉/终端电阻，`GPIO_PULLUP` 仅作辅助
- RS485 A/B 线建议使用双绞线，降低电磁干扰影响
- 舵机供电建议独立于 MCU 电源，避免 PWM 切换时的电源波动干扰

***

## 🎯 下一步演进方向

### 已完成功能（v1.7）
- ✅ Modbus RTU 从机（0x03/0x06 功能码）
- ✅ UART 接收链防护与错误恢复机制
- ✅ 系统心跳与看门狗自恢复
- ✅ 双路异构舵机控制（180°/360° SG90）
- ✅ 代码规范化（消除魔法数字、Doxygen 注释、.clang-format）

### 计划中功能（v1.8+）
- [ ] 接入 AHT20（温湿度）+ BMP280（气压）传感器，实现 I2C 数据采集闭环
- [ ] 接入电位器/编码器，实现本地手动控制模式
- [ ] 扩展 Modbus 主站功能，实现双 MCU 主从架构（STM32 + ESP32）
- [ ] 增加 Wi-Fi/MQTT 网络协议栈，实现云端数据上报
- [ ] 完善长稳测试（24h+ 连续运行）与边界值压力测试
- [ ] 增加 OTA 固件升级功能
