# 工作日志_modbus网关项目

## 2026-05-20 | Modbus协议层异常响应修正与360°舵机死区优化（Commit: `待生成`）

### ✅ 完成内容

1. **Modbus协议层异常响应修正（P3-001）**
   - 在 `Modbus_WriteHoldingRegister()` 添加前置校验，非法值（角度>180/速度>255）返回 false → 触发 0x03 异常帧
   - 简化回调函数，移除冗余范围检查，明确职责分层（协议层校验、驱动层执行）
   - 验证：✅ 写入非法值500返回异常码0x03，合法值90原样回显且舵机正常转动

2. **360°舵机死区保护优化（P3-002）**
   - 将死区范围从±5档（122-132）扩大到±10档（117-137），对应PWM变化约±80μs
   - 更新宏定义：`REV_LIMIT=117`, `FWD_LIMIT=137`，解决长时间运转后的机械卡顿问题
   - 验证：✅ 15+分钟边界测试无卡顿，反转边界0x75（117）、正转边界0x89（137）稳定运行

3. **文档同步更新**
   - 更新寄存器映射表、测试指南中的死区边界值和测试指令，新增CRC速查表
   - 新增问题记录 P3-001/P3-002 条目，记录根因分析与修复方案
   - 验证：✅ Keil MDK全量编译通过，功能回归测试通过

### 🐛 问题记录

**已解决**：[P3-001] Modbus写入非法值未返回异常码；[P3-002] 360°舵机死区范围不足导致卡顿  
**设计原则**：协议层负责参数合法性校验，驱动层负责物理执行；死区范围需根据实际硬件环境动态调整

### 🎯 后续计划

\[待评估] 分支结束时机：当前两个P3级问题已修复并验证通过，本分支可合并至main  
\[待完成] Git提交：按原子性原则分2次提交（协议层修复 + 死区优化+文档）

***

## 2026-05-19 | 日志开关解耦重构与看门狗日志精简（Commit: `待生成`）

### ✅ 完成内容

1. **日志函数拆分与职责分离**
   - 新增 `System_Error_Log()` 函数：输出关键错误日志（配置校验失败、任务创建失败、复位检测等），受 [SYSTEM_UART_TEXT_LOG_ENABLE](file://d:\STM32\myproject\ModbusRTU_Gateway\App\System\Inc\system_config.h#L20-L21) 控制，生产环境默认启用
   - 修改 `System_Monitor_Log()` 函数：仅输出监控日志（栈水位打印/告警/恢复），受 [SYSTEM_STACK_WATERMARK_LOG_ENABLE](file://d:\STM32\myproject\ModbusRTU_Gateway\App\System\Inc\system_config.h#L17-L18) 独立控制，生产环境建议关闭
   - 架构收益：实现错误诊断日志与周期性监控日志的解耦，既保留故障追溯能力，又减少 UART 负载

2. **关键错误日志迁移**
   - 配置校验失败（7 处）：改用 `System_Error_Log()`，确保 fail-fast 机制始终可见
   - 任务创建失败（3 处）：修正错误码为 `SYSTEM_ERR_TASK_CREATE_LED/UART/MONITOR`，改用 `System_Error_Log()`
   - 复位原因检测（6 类）：IWDG/WWDG/POR/PIN/SOFT/LPWR 复位标志，改用 `System_Error_Log()`
   - 首次上电提示：改用 `System_Error_Log()`，明确系统启动状态

3. **看门狗日志精简**
   - 删除看门狗初始化成功日志（正常流程无需输出）
   - 保留看门狗初始化失败日志（严重错误必须记录）
   - 验证结果：✅ Keil MDK 全量编译通过，无错误无警告；功能回归测试通过（Modbus 读写/舵机控制/看门狗喂狗）

4. **文档同步更新**
   - 更新 [system_config.h](file://d:\STM32\myproject\ModbusRTU_Gateway\App\System\Inc\system_config.h) 注释：明确两个宏的职责分工与生产环境推荐配置
   - 关闭 P2-006 问题：标记为"✅ 已修复（2026-05-19）"

### 🐛 问题记录

**已解决**：
- [P2-006] 系统日志开关职责耦合 → ✅ 已修复，拆分为独立控制宏
- 编译错误：`SYSTEM_ERR_TASK_CREATE_FAILED` 未定义 → ✅ 修正为具体任务错误码枚举

**设计原则**：
- 错误日志（Error Log）→ 始终启用，用于故障诊断
- 监控日志（Monitor Log）→ 可选关闭，用于性能优化
- 看门狗初始化成功 → 不输出（预期行为）

### 🎯 后续计划

- \[待完成] 传感器模块接入（AHT20温湿度 + BMP280气压）：硬件已到，可开始开发
- \[待完成] 分支结束时机评估：传感器数据接入后，本分支可合并至main

***

## 2026-05-17 | 全项目注释规范化与工程规范文档发布

### ✅ 完成内容

1. **全项目注释规范化（6层25个文件）**
   - **BSP层**（6个文件）：uart.h/led.h/servo.h 宏定义按功能分组，公共API添加Doxygen注释；uart.c/led.c/servo.c 关键逻辑添加设计意图注释
   - **Driver层**（4个文件）：driver_uart.h/driver_servo.h 公共API完整Doxygen注释；driver_uart.c/driver_servo.c L2设计意图注释（懒加载互斥锁、速度线性映射公式）
   - **Protocol层**（2个文件）：modbus.h 22个帧索引宏按请求/响应/异常分组；modbus.c 协议解析关键逻辑添加参考文档标注
   - **System层**（5个文件）：system_config.h/error_handler.h/system_ctrl.h 宏定义按四类分组；error_handler.c/system_ctrl.c L2设计意图注释（中断上下文跳过日志、优雅退出机制、配置校验防御性编程）
   - **Task层**（7个文件）：led_task.h/uart_task.h/monitor_task.h 公共API Doxygen注释；led_task.c/uart_task.c/monitor_task.c L2设计意图注释（优雅退出、背靠背请求优化、三合一后台任务）
   - **Core层**（1个文件）：main.c 系统启动流程7步骤说明，时钟树配置注释
   - 验证结果：✅ Keil MDK 全量编译通过，无新增错误和警告；功能回归测试通过（Modbus读写/舵机控制/看门狗喂狗）
2. **工程规范文档发布**
   - 创建 [CODING_STYLE.md](file://d:\STM32\myproject\ModbusRTU_Gateway\CODING_STYLE.md)：嵌入式 C 代码工程规范（格式化/注释/命名/架构/错误处理/Git提交），350+ 行完整文档
   - 更新 [Readme.md](file://d:\STM32\myproject\ModbusRTU_Gateway\Readme.md)：新增"代码工程化规范"技术亮点章节，版本号升级至 v1.8.0
   - 更新 [协议说明.md](file://d:\STM32\myproject\ModbusRTU_Gateway\docs\协议说明.md)：文档版本升级至 V1.8，更新后续开发计划（传感器接入）
3. **问题记录同步**
   - 在 [问题记录.md](file://d:\STM32\myproject\ModbusRTU_Gateway\docs\问题记录.md) 中新增 P2-006 日志开关耦合问题，标记为"📝 已记录（2026-05-17）"
   - 建议在独立分支 `refactor/log-channel-decoupling` 处理日志开关拆分

### 🐛 问题记录

**新发现**：
- [P2-006] 系统日志开关职责耦合，[SYSTEM_UART_TEXT_LOG_ENABLE](file://d:\STM32\myproject\ModbusRTU_Gateway\App\System\Inc\system_config.h#L17-L18) 同时控制错误日志和监控日志，违反单一职责原则

**讨论结论**：
- 错误日志应始终启用（故障诊断必需），但需精简输出格式（去除文件路径，仅保留模块ID+行号）
- 当前分支不修改代码，问题记录至台账，待独立分支处理

### 🎯 后续计划

- \[待完成] 传感器模块接入（AHT20温湿度 + BMP280气压）：硬件已到，可开始开发
- \[待完成] 分支结束时机评估：传感器数据接入后，本分支可合并至main
- \[待完成] 独立分支 `refactor/log-channel-decoupling`：处理日志开关拆分问题

***

## 2026-05-16 | 代码规范化阶段二 - 宏定义模块化自治与编译警告消除（Commit: `c61fdbe`）

### ✅ 完成内容

1. **驱动层与应用层魔法数字清理**
   - [servo.h](file://d:\STM32\myproject\ModbusRTU_Gateway\Bsp\Inc\servo.h) / [driver_servo.h](file://d:\STM32\myproject\ModbusRTU_Gateway\App\Driver\Inc\driver_servo.h)：提取舵机脉宽、速度映射等物理参数至模块头文件
   - [system_ctrl.c](file://d:\STM32\myproject\ModbusRTU_Gateway\App\System\Src\system_ctrl.c) / [error_handler.c](file://d:\STM32\myproject\ModbusRTU_Gateway\App\System\Src\error_handler.c)：将内部缓冲区大小、超时时间等提取为文件级宏
   - 验证结果：✅ 全工程编译通过，无错误，无重定义警告
2. **BSP 驱动层代码格式化**
   - 创建 `.clang-format` 配置文件（K&R 大括号风格、4 空格缩进、连续赋值不对齐）
   - 格式化 BSP 层 10 个文件（5 个头文件 + 5 个源文件：led/uart/servo/key/exti）
   - 验证结果：✅ Keil 编译通过，功能测试正常
3. **紧急修复：Modbus 0x06 响应格式错误 BUG**
   - 问题根因：0x06 成功响应使用了错误的宏定义（`MODBUS_RESP_DATA_START_IDX=3`）
   - 导致现象：响应帧多出字节计数字段，返回 9 字节而非标准的 8 字节
   - 修复方案：改用请求帧索引宏（`MODBUS_REQ_REG_ADDR_HIGH_IDX=2`）构建响应
   - 测试验证：0x06 写入全部原样回显，0x03 读取功能正常

###  问题记录

关联台账问题：[P1-002] 代码大量存在魔法数字及配置职责混乱\
当日进展：✅ 已完成全工程魔法数字清理，消除了硬编码索引与业务阈值，编译环境纯净

###  后续计划

- [待完成] 规范注释，补充 Doxygen 风格精简版函数说明、参数描述、返回值说明
- [待完成] 格式规范化，按照.clang-format格式化代码

***

## 2026-05-14 | 代码规范化阶段一 - 消除魔法数字与配置优化（Commit: `待生成`）

### ✅ 完成内容

1. **Modbus 协议层魔法数字消除**
   - 在 [modbus.h](file://d:\STM32\myproject\ModbusRTU_Gateway\App\Protocol\Inc\modbus.h) 中新增 22 个帧字段索引宏
   - 替换 [modbus.c](file://d:\STM32\myproject\ModbusRTU_Gateway\App\Protocol\Src\modbus.c) 中所有硬编码索引（验证：`grep "frame\[\d+\]" modbus.c` → 0 matches）
2. **业务逻辑参数规范化**
   - [led\_task.c](file://d:\STM32\myproject\ModbusRTU_Gateway\App\Task\Src\led_task.c) LED 翻转周期：`osDelay(500)` → `osDelay(LED_TASK_TOGGLE_INTERVAL_MS)`
   - [system\_ctrl.c](file://d:\STM32\myproject\ModbusRTU_Gateway\App\System\Src\system_ctrl.c) 8 处栈/缓冲区校验阈值硬编码替换为宏定义（新增 8 个校验宏）
3.  **配置文件职责分离深化**
   - 严禁向 [system_config.h](file://d:\STM32\myproject\ModbusRTU_Gateway\App\System\Inc\system_config.h) 塞入非全局配置，确保各模块“自产自销”
   - 修正 [error_handler.h](file://d:\STM32\myproject\ModbusRTU_Gateway\App\System\Inc\error_handler.h) 中宏定义重复问题，统一常量出口
   - 架构收益：提升代码内聚性，后续修改特定硬件参数只需定位到对应模块头文件
4. **文档同步更新**
   - 同步更新 [协议说明.md](file://d:\STM32\myproject\ModbusRTU_Gateway\docs\协议说明.md)、[问题记录.md](file://d:\STM32\myproject\ModbusRTU_Gateway\docs\问题记录.md)
   - 更新 P1-002 状态为"✅ 已修复"，补充实现细节与验证结果

### ⚠️ 问题与解决

关联台账问题：\[P1-002] 代码大量存在魔法数字\
当日进展：✅ 已完成协议层 + 配置层改造，编译通过，烧录成功，循环混合指令测试通过

### 🎯 后续计划

- \[待完成] 第三步"注释规范化"：补充 Doxygen 风格注释，完善函数说明、参数描述、返回值说明
- \[待完成] 应用 `.clang-format` 全项目格式化

***

## 2026-05-13 | PR #3 合并与代码规范化阶段（Commit: `9e47b8f` / `539365e`）

### ✅ 完成内容

1. **系统心跳功能实现**
   - 在 [Monitor\_Task](file://d:\STM32\myproject\ModbusRTU_Gateway\App\Task\Src\monitor_task.c) 中实现 `0x0002` 寄存器低 4 位每秒翻转逻辑（`0x000F ↔ 0x0000`）
   - 增加内部专用写入接口 [Modbus\_InternalWriteRegister](file://d:\STM32\myproject\ModbusRTU_Gateway\App\Protocol\Src\modbus.c#L356-L371)，解决只读标志冲突
   - 修正翻转算法为纯异或操作 `current_val ^ 0x000FU`，修复原逻辑语义错误
   - 解耦延时与业务逻辑：心跳 1000ms、喂狗 500ms、栈检查 60s 独立管理，基础循环周期降至 10ms
2. **压力测试验证通过**
   - 9 分钟高频混合测试（540 帧，100% 响应成功率）
   - 验证场景：心跳寄存器翻转 + 180° 舵机最大位置切换 + 360° 舵机正反转调速 + 非法功能码容错
   - 结果：心跳规律跳变、双路舵机无卡顿、异常帧正确返回、栈水位稳定、MCU 温升 ≤ 3℃
3. **PR #3 合并与功能收口**
   - 完成 Pull Request #3 合并：`feat: 实现双路异构舵机控制与系统心跳机制`
   - 更改统计：17 个文件变更，494 行新增，73 行删除
   - 合并策略：保持主干历史清晰，采用标准合并流程
   - 系统状态：`main` 分支已包含所有已验证功能，处于稳定基线状态

### ⚠️ 问题记录

- 初始翻转逻辑 `(current_val & 0xFFF0U) ^ 0x000FU` 存在语义错误，第二次执行结果不变
- 任务延时耦合问题：`STACK_WATERMARK_LOG_DELAY` 设为 1000ms 导致心跳周期变为 10s，已通过时间片解耦修复
- PWM 切换时存在单周期波形毛刺（硬件定时器特性），但机械负载惯性可完全过滤

### 🎯 后续计划

- \[待完成] 代码规范化清理：魔法数字宏定义、Doxygen注释补充、任务栈水位监控、UART临界区复查（分支 `refactor/code-standardization`，预计2-3天）
- \[待完成] I2C传感器与ADC本地控制：AHT20/BMP280接入 + 电位器本地控制（分支 `feature/i2c-sensor-local-ctrl`，预计3-5天）

***

## 2026-05-12 | Step 1: 寄存器回调映射与双舵机驱动实现（Commit: `ab1ead8` / `a85f83f`）

### ✅ 完成内容

1. **最小闭环验证 (Step 1)**
   - 实现 Modbus 寄存器 `0x0004` 到 PWM 占空比的回调映射，打通 `Protocol -> Driver -> Hardware` 数据流。
   - 调整舵机控制引脚至 PA6 (TIM3\_CH1)，并预留 PA7 (TIM3\_CH2) 支持双通道控制。
   - 优化架构解耦：将 `Error_Handler` 下沉至系统层，解决 BSP 层对 Core 层的依赖问题。
   - **测试结论**：示波器观测 PA6 波形，频率稳定 50Hz；写入 0/90/180 度，占空比分别为 2.5%/7.5%/12.5%，符合预期，连接180°sg90舵机信号脚测试，功能正常。
2. **360° 连续旋转舵机控制**
   - 实现 Modbus 寄存器 `0x0005` (PA7/TIM3\_CH2) 到 360° 舵机的速度/方向映射。
   - 完成中位死区处理：值 127 对应 1.5ms 脉宽（停止），0\~126 为反转，128\~255 为正转。
   - 在 BSP 层增加 [BSP\_Servo\_SetPulseWidth](file://d:\STM32\myproject\ModbusRTU_Gateway\Bsp\Src\servo.c#L67-L84) 接口，支持微秒级精确脉宽控制。
3. **PWM 切换抖动根因分析**
   - **现象**：占空比切换瞬间出现 1 个周期的波形毛刺（频率偏差 45Hz\~57Hz）。
   - **结论**：确认为 STM32 定时器影子寄存器更新机制导致的硬件特性，非软件缺陷。由于持续时间 <20ms，舵机机械惯性可完全过滤，不影响功能。

4**系统配置修复**

- 修正 [MONITOR\_TASK\_STACK\_SIZE](file://d:\STM32\myproject\ModbusRTU_Gateway\App\System\Inc\system_config.h#L9-L9) 为 192，解决系统启动时的配置校验失败问题。

### ⚠️ 问题记录

- 启用 HAL TIM 模块时需注意同步开启 UART 模块，避免配置冲突导致编译报错。
- STM32F1 系列高级定时器扩展回调需通过宏定义屏蔽，以消除链接阶段的符号未定义错误。

### 🎯 后续计划

- \[待完成] 增加系统心跳寄存器 (`0x0002`)，提供 MCU 存活及调度正常的直观证据。
- \[待完成] 运行 12 分钟高频读写压力测试，验证双路 PWM 下的通信稳定性。

***

## 2026-05-10 | PR 提交与合并阶段（fix/system-defense-hardening 分支, Commit: `f1e3b7f` / `94fc20a` / `97dabbc`）

### ✅ 完成内容

1. **PR 创建与提交**
   - 创建 Pull Request #2：`fix(system): 系统防御加固（参数校验/看门狗/UART 错误恢复/栈配置优化）`
   - 分支来源：`fix/system-defense-hardening` → `main`
   - 提交次数：共 5 次提交
   - 更改文件：23 个文件变更
2. **PR 描述编写**
   - 采用简洁工程化表述，去除表情符号和复杂 Markdown 格式
   - 按"问题 - 修复 - 测试"三段式描述 4 个核心问题
   - 诚实标注未验证项（P0-001/P1-004 需真实硬件环境验证）
   - 包含风险评估和注意事项
3. **PR 合并**
   - PR #2 已成功合并到 main 分支
   - 合并方式：Squash and Merge（保持主干历史清晰）
   - 合并时间：提交后 19 小时
4. **文档体系补充**
   - 新增 `docs/协议说明.md`：规范化 CRC-16 字节序说明（低字节在前，高字节在后）
   - 更新 `Readme.md`：添加版本号 v1.5.0，同步任务栈配置数据
   - 新增开源规范文件：LICENSE、CHANGELOG.md、CONTRIBUTING.md、DEPENDENCIES.md、.clang-format

### ⚠️ 问题记录

- PR 描述初期版本过长且使用了表情符号，后经调整简化为纯文本工程化表述
- 本分支与 main 存在合并冲突，已手动解决后合并

### 后续计划

- \[待完成] 合并后进入第三阶段：DHT11 温湿度传感器 + SG90 舵机外设接入
- \[待完成] 独立分支处理代码规范化（应用 .clang-format 全项目格式化）
- \[待完成] P1、P2级其他问题修复与验证

***

## 2026-05-09 | P1-004 UART 硬件错误分级处理实现（Commit: `b267452`）

### ✅ 完成内容

1. **BSP层 - UART 错误分级处理机制**
   - 实现 ORE/FE/NE/PE 错误分类处理逻辑
   - 涉及文件：`Bsp/Inc/uart.h`、`Bsp/Src/uart.c`
   - 核心变更：
     - ORE（溢出错误）：立即触发恢复
     - FE/NE（帧/噪声错误）：连续 3a'aaaa 次触发恢复
     - PE（校验错误）：连续 5 次触发恢复
     - 新增 `s_uart_fe_streak` 和 `s_uart_pe_streak` 连续错误计数器
     - 错误清除时同步重置所有连续计数器
   - 验证结果：✅ 编译通过，基础通信功能正常（0x03/0x06 读写验证通过）
2. **文档同步 - 问题记录更新**（核心亮点）
   - 设计思路：诚实标注测试局限性，区分代码逻辑正确性与物理验证条件
   - 代码变更：更新 `docs/问题记录.md` 中 P1-004 状态为"已修复"
   - 验证结果：✅ 文档与代码实现状态一致

### ⚠️ 问题记录

- **P0-001/P1-004 测试验证局限性**
  - 问题现象：物理断线、代码注入均无法可靠触发 UART 硬件错误标志位（ORE/FE/NE）
  - 根因分析：STM32 HAL 库特性，实验室环境无法模拟工业现场电磁干扰
  - 当前状态：✅ 代码逻辑已实现并审查通过，真实硬件故障场景需工业环境验证

### 🎯 后续计划

- \[待完成] 合并后进入第三阶段：DHT11 温湿度传感器 + SG90 舵机外设接入
- \[待完成] 清理文档中重复冗余的内容

***

## 2026-05-08 | P0-001UART 错误恢复死锁/P0-002看门狗功能缺失 稳定性修复与验证（Commit: `8025bad`）

### ✅ 完成内容

1. **P0-001 UART 错误恢复死锁隐患修复**

- 实现重试熔断机制：增加 `s_recovery_retry_count` 计数器，设定阈值 `UART_MAX_RECOVERY_RETRY = 10`，超限后标记 `s_uart_hw_fault = true` 并停止自动恢复，测试时未进入死锁状态，无法继续触发恢复逻辑。
- 增加自愈逻辑：故障标记后连续接收 5 帧则清除故障标记，恢复正常恢复逻辑，测试时未成功触发。
- 故障反馈：触发熔断后 LED 执行 5 次急促快闪（50ms 间隔），调用 `ErrorLogRecord(ERROR_SYSTEM, ...)` 记录硬件故障，但测试时未成功触发。
- 代码变更：`Bsp/Inc/uart.h` 增加阈值宏定义；`Bsp/Src/uart.c` 重构恢复逻辑，包含 `led.h` 解决隐式声明警告。
- 测试结论：常规通信及物理断线场景下 UART 硬件未触发错误标志位，恢复逻辑静默；代码注入测试验证了熔断计数器累加逻辑，真实硬件故障场景需在工业干扰环境下进一步验证。

1. **P0-002 看门狗功能集成与故障模拟验证**

- 看门狗驱动与时钟配置：在System\_IWDG\_Init()中开启 LSI 时钟，配置预分频 256、重载值 312，实现精准 2 秒超时；在Monitor\_Task中实现 100ms 周期喂狗，确保正常运行不触发复位。
- 故障模拟与验证：通过喂狗后进入死循环模拟系统跑飞，经逻辑分析仪、寄存器调试、串口日志验证，系统 2 秒准时复位，5 秒钟监测周期稳定无偏差。
- 超时参数修复：修正原重载值 4095 导致的 26 秒超长超时问题，按公式重新计算参数，满足工业设备快速恢复要求。
- 代码与文档清理：删除测试死循环代码，恢复正常业务流程；更新问题记录.md，将 P0-002 状态标记为已验证通过。

### ️ 问题记录

- P0-001 熔断机制代码已集成，但真实硬件故障场景下的触发条件尚未通过物理测试完全验证。

### 🎯 后续计划

- \[待完成] 补充故障模拟测试，验证熔断与自愈逻辑有效性
- \[待完成] 增加看门狗复位原因检测（`RCC->CSR` IWDGRSTF 标志位）
- \[待完成] 处理 P1 级问题（魔法数字替换、CRC 查表优化）

***

## 2026-05-07 | P1-001 参数校验修复测试与代码清理阶段（Commit: `34a02de`）

### ✅ 完成内容

1. **P1-001 参数校验修复验证**

- 完成核心对外接口入参有效性校验的单元测试，覆盖空指针、地址越界、长度超限、非法数值等场景
- 测试工具：自定义 LED 闪烁反馈机制 + 条件编译开关 [ENABLE\_P1\_001\_TEST\_MODE](file://d:\STM32\myproject\ModbusRTU_Gateway\App\System\Inc\system_config.h#L40-L41)
- 测试结果：7 个核心测试用例全部通过，LED 快速闪烁 3 次反馈验证成功
- 压力测试：以 100ms/帧频率连续发送 0x03 和 0x06 指令 12 分钟，MCU 温升正常（比室温高3℃），LED 规律闪烁，串口无丢包乱码
- 栈水位监控：各任务栈剩余空间均高于初始值 20% 安全阈值，无溢出风险

1. **看门狗功能代码集成**

- 完成 IWDG 看门狗驱动代码集成，配置预分频 256、重装载值 4095（约 2 秒超时）
- 在 Monitor\_Task 中实现周期性喂狗逻辑，确保系统正常运行时不触发复位
- 已完成编译验证，待后续进行"故意阻塞喂狗"的故障模拟测试

1. **测试代码清理与归档**

- 移除散落在业务代码里的测试开关、测试调用，避免残留以及误触发测试逻辑
- 将 `App/Test/` 提交到git仓库，保留测试逻辑供后续参考
- 完善 `.gitignore` 规则，避免将keil项目编译产物提交到git仓库

### ⚠️ 问题记录

- 看门狗功能已集成但未进行故障模拟测试（需验证"不喂狗时系统是否自动复位"）
- `App/Test/` 测试文件已归档但未从 Git 索引中移除，需在下次提交前执行 `git rm --cached` 清理

### 🎯 后续计划

- \[待完成] 执行看门狗故障模拟测试：故意阻塞喂狗任务，验证系统 2 秒内自动复位
- \[待完成] 按照 P0-P1 优先级，开始处理P0-001 UART 错误恢复逻辑存在死锁隐患以及 P1 级其他问题（魔法数字替换、CRC 优化等）

***

## 2026-05-06 | Git分支管理与测试流程梳理阶段（Commit: `5f3d579` / `a33092d`）

### ✅ 完成内容

1. **分支管理流程梳理**

- 明确了Git分支管理最佳实践：每个问题创建独立分支（如fix/P1-004-param-validation）
- 理解了分支树的形成机制：需要通过Pull Request/Merge Request进行合并，而非本地直接合并
- 梳理了从修复问题到测试验证的完整流程：新建分支 → 修复代码 → 补充测试 → 提交推送 → 发起PR → 审核合并

1. **测试框架准备**

- 在App/Test/目录下创建test\_validation.c测试文件
- 为P1-004参数校验问题准备了6个核心测试用例
- 包含地址越界、值范围校验、只读保护、正常操作、空指针防护、长度溢出等场景
- 为后续的自动化测试奠定基础

1. **修复分支状态同步**

- 将main分支的最新代码合并到fix/stability-uart-crc修复分支
- 确保修复分支包含最新的主干变更，为后续测试做准备
- 保持修复分支与主干同步，避免集成冲突

### ⚠️ 问题记录

- 之前对Git分支管理理解不足，仅在本地创建分支后直接合并到main，未使用PR流程
- 没有为修复的P1-004问题创建独立分支，导致分支历史不够清晰
- 缺乏系统的测试用例编写，之前主要依赖手动联调验证

### 后续计划

- \[待完成] 明天正式开始P1-004参数校验修复的测试验证
- \[待完成] 编译运行test\_validation.c中的测试用例，验证修复效果
- \[待完成] 按照标准流程提交测试代码和修复代码
- \[待完成] 推送分支并发起PR，要求review和CI自动测试
- \[待完成] 合并前进行全量回归测试，确保主干稳定
- \[待完成] 按照P0-P1优先级顺序，依次修复P0-001看门狗机制和P0-002 UART死锁隐患

***

## 2026-05-05 | Modbus 0x06功能码最小闭环开发与修复分支规划阶段（Commit: `890d6d6`）

### ✅ 完成内容

1. **创建修复分支并梳理当前问题**

- 创建 `fix/tech-debt-resolve-main-issues` 分支，专门处理P0/P1级别技术债
- 详细梳理 `问题记录.md` 中列出的所有问题，按优先级排序
- 重点分析P0级问题：看门狗机制缺失、UART错误恢复死锁隐患
- 确认当前main分支已包含0x06功能码完整实现，协议层面功能闭环
- 确认UART接收链优化已基本完成，背靠背请求窗口问题已解决
- 确认协议层分层架构已加固，降低耦合风险

1. **测试验证与文档同步**

- 根据测试记录.md中的用例，完成了所有基础测试项验证
- 更新分阶段任务.md，将"扩展 Modbus 功能码"阶段标记为已完成
- 补充工作日志\_modbus网关项目.md，记录本次开发过程与关键决策

### ⚠️ 问题记录

- P0级问题（看门狗、UART死锁）必须立即处理，否则系统不具备工业应用可靠性
- P1级问题（参数校验、魔法数字、错误处理）影响代码质量和长期维护性

### 🎯 后续计划

- \[待完成] 解决P0级问题：实现硬件看门狗机制和UART错误恢复逻辑死锁隐患
- \[待完成] 逐步处理P1级问题：参数校验、魔法数字替换、CRC优化等

***

## 2026-05-04 | Modbus 0x06功能码核心实现与协议层加固阶段

### ✅ 完成内容

1. **Modbus 0x06功能码核心实现**

- 在modbus.c中实现了Modbus\_HandleWriteSingleReg()函数，完整支持0x06功能码的解析与处理
- 实现了合法地址检查、值范围校验、只读寄存器保护等关键逻辑
- 成功回显请求帧作为成功响应，符合Modbus协议规范

1. **异常响应机制完善**

- 针对越界地址、非法值、只读寄存器等情况，返回标准异常帧（0x86 + 异常码）
- CRC错误帧保持无响应策略，避免无效帧干扰系统

1. **协议层分层架构加固**（核心亮点）

- 将协议层自身的发送超时配置从BSP层移出，统一定义于[modbus.h](file://d:\STM32\myproject\ModbusRTU_Gateway\App\Protocol\Inc\modbus.h)头文件中
- **分层设计优势**：
  - BSP层仅负责硬件抽象
  - Driver层仅封装RTOS侧接口
  - Protocol层仅依赖驱动抽象，不直接引用BSP常量
- 通过此变更，增强协议层独立性，降低耦合风险

### ⚠️ 问题记录

- 0x06功能码虽已实现，但尚未接入真实外设（如SG90舵机），仍处于纯寄存器写回阶段
- UART接收链的背靠背请求窗口问题仍在观察中，需继续优化
- 当前main分支虽然功能可用，但存在潜在稳定性风险，不适合直接部署
- 修复过程中需确保不影响现有的Modbus 0x03/0x06功能

### 后续计划

- \[待完成] 完成修复后进行全面回归测试，确保功能与稳定性并存
- \[待完成] 进行0x06写寄存器功能完整回归测试，通过后将feature/modbus-0x06-minimal分支合并至main
- \[待完成] 开始第三阶段开发：接入DHT11温湿度传感器和SG90舵机，实现真正的外设控制闭环
- \[待完成] 评估是否需要增加按键输入接口用于本地控制

***

## 2026-05-02 | UART接收链优化与写寄存器规则冻结阶段（Commit: `9a48233` / `d123f30`）

### ✅ 完成内容

1. **UART接收链优化**

- 修复了UART背靠背请求窗口问题，通过显式调用`HAL_UART_AbortReceive()`并重新挂接接收，确保每次接收完成后都能正确重置接收状态
- 减少了UART各固定轮询延迟，将[uart\_task.c](file://d:\STM32\myproject\ModbusRTU_Gateway\App\Task\Src\uart_task.c)任务循环中的[osDelay(1)](file://d:\STM32\myproject\ModbusRTU_Gateway\Drivers\CMSIS\RTOS\Template\cmsis_os.h#L331-L331)改为"有帧待处理时立刻继续，无帧时再休眠"，从而缩小了任务侧人为引入的等待窗口
- 这些变更有效减少了背靠背请求场景下的响应延迟，提升了通信稳定性

1. **冻结当前寄存器映射与规则冻结**

- 冻结当前寄存器映射表：舵机目标寄存器（0x0004）可写，范围0\~180；温度传感器（0x0000）只读
- 明确写入合法性判断规则，为后续外设接入提供清晰接口
- 第一轮仅开放一个可写寄存器（0x0004），用于验证功能面：
  - 作为舵机目标角度占位
  - 默认值设为90
  - 合法范围严格限定在0 \~ 180
- 此设计确保后续开发中，成功路径、越界路径、只读路径、非法值路径均有明确处理方式，避免边写代码边改需求

### ⚠️ 问题记录

- `Modbus` 寄存器访问层和 UART 恢复路径的一轮基础修复已经完成，但接收链在背靠背请求、IDLE 时序和中断边界上的问题还没有完全结束
- "按键触发单次栈水位上报"这个思路可以保留，但当前不应绕过 USART1 日志隔离直接加入主线，应作为 `0x06` 最小闭环稳定后的维护增强项单独设计

### 🎯 后续计划

- `[待完成]` 在开始 `0x06` 前，冻结最小写寄存器规则：可写地址、只读地址、异常码口径、成功回显规则
- `[待完成]` 当前接收链进一步优化后，再正式进入 `0x06` 最小闭环开发，只做纯寄存器写回，不混入外设联动
- `[想法1]` 后续可增加"按键触发一次当前栈水位上报"的维护入口；但该能力不应在当前阶段绕过日志隔离直接污染 Modbus UART，建议放在 `0x06` 最小闭环稳定后，再按"按键触发单次上报 + 低水位告警立即上报 + 恢复后上报一次恢复信息"的思路单独设计与验证

***

## 2026-04-30 | 仓库整理与问题修复阶段（Commit: `03159d8` / `f918641` / `63c9b67` / `71afb78` / `f9f84f8` / `9a48233`）

### ✅ 完成内容

1. **仓库整理与构建产物清理**

- 完成优化分支合并后的主线复查，确保本次合并为快速前进(fast-forward)合并，没有额外的合并提交
- 清理误跟踪进仓库的 `MDK-ARM/ModbusRTU_Gateway/` 构建产物，并删除历史操作带入的异常文件名文件
- 更新根目录 `.gitignore`，补充 `Keil/MDK-ARM` 常见生成物忽略规则，避免后续再次把 [.axf](file://d:\STM32\myproject\ModbusRTU_Gateway\MDK-ARM\ModbusRTU_Gateway\ModbusRTU_Gateway.axf)、[.hex](file://d:\STM32\myproject\ModbusRTU_Gateway\MDK-ARM\ModbusRTU_Gateway\ModbusRTU_Gateway.hex)、[.map](file://d:\STM32\myproject\ModbusRTU_Gateway\MDK-ARM\ModbusRTU_Gateway\ModbusRTU_Gateway.map)、[.o](file://d:\STM32\myproject\ModbusRTU_Gateway\MDK-ARM\ModbusRTU_Gateway\key.o)、[.d](file://d:\STM32\myproject\ModbusRTU_Gateway\MDK-ARM\ModbusRTU_Gateway\key.d) 等文件带入版本库
- 将清理提交的说明从全英文改为与当前仓库风格一致的中文描述，便于后续回看提交历史

1. **寄存器访问并发保护与** **`0x03`** **一致性修复**

- 在新分支 `tech-debt/issue-record-followup` 上完成 `holding_regs` 访问互斥保护，覆盖读寄存器、写寄存器和回调注册入口
- 将写寄存器后的 `on_change` 回调改为锁外执行，避免后续扩展时出现锁内回调导致的阻塞或重入风险
- 将多寄存器读响应改为"单次加锁快照读取"，避免一帧响应中前后寄存器值来自不同时间点
- 该优化不改变当前 `0x03` 协议行为，但为后续接入 `0x06` 写寄存器路径提前规范一致性边界

1. **问题文档整理**

- 将 `问题记录.md` 重写为"当前有效问题清单"，只保留会影响下一阶段开发和联调结论的真实问题
- 将 `已解决问题清单.md` 精简为 3 个有展示价值的已解决案例，删除重复、过时、展示价值低的条目

1. **Modbus 总线日志污染修复**

- 在 [system\_config.h](file://d:\STM32\myproject\ModbusRTU_Gateway\App\System\Inc\system_config.h) 增加 [SYSTEM\_UART\_TEXT\_LOG\_ENABLE](file://d:\STM32\myproject\ModbusRTU_Gateway\App\System\Inc\system_config.h#L26-L27) 编译期开关，当前默认关闭 USART1 上的 ASCII 文本日志发送
- 规范 [system\_ctrl.c](file://d:\STM32\myproject\ModbusRTU_Gateway\App\System\Src\system_ctrl.c) 和 [error\_handler.c](file://d:\STM32\myproject\ModbusRTU_Gateway\App\System\Src\error_handler.c) 的文本日志出口，保证当前联调默认只在总线上跑 Modbus 帧，不再混入栈水位、错误提示等文本输出

1. **UART 接收链第一轮修复**

- 已整理 [uart.c](file://d:\STM32\myproject\ModbusRTU_Gateway\Bsp\Src\uart.c) 中"正常取帧后重启接收"和"错误恢复重启接收"两套不同逻辑，统一到底层同一类 `AbortReceive + 重新挂接接收` 的模型
- 最小回归测试未见明显异常，已完成单独提交，接收恢复策略比之前更一致，后续继续观察长时间联调表现
- 调整 [uart\_task.c](file://d:\STM32\myproject\ModbusRTU_Gateway\App\Task\Src\uart_task.c) 任务循环，不再每轮固定 [osDelay(1)](file://d:\STM32\myproject\ModbusRTU_Gateway\Drivers\CMSIS\RTOS\Template\cmsis_os.h#L331-L331)，改为"有帧待处理时立刻继续，无帧时再休眠"，缩小任务侧人为引入的等待窗口
- 该修改主要针对背靠背请求场景，尽量减少 `IDLE` 判帧后又额外等待 1ms 才开始处理的问题，已完成单独提交

### ⚠️ 问题记录

- `问题记录.md` 中"Modbus 总线日志污染"和"UART 恢复路径不一致"已完成当前轮修复，并已补记当前状态
- `Modbus` 寄存器访问层和 UART 恢复路径的一轮基础修复已经完成，但接收链在背靠背请求、IDLE 时序和中断边界上的问题还没有完全结束
- "按键触发单次栈水位上报"这个思路可以保留，但当前不应绕过 USART1 日志隔离直接加入主线，应作为 `0x06` 最小闭环稳定后的维护增强项单独设计

### 🎯 后续计划

- `[待完成]` 继续处理 [uart.c](file://d:\STM32\myproject\ModbusRTU_Gateway\Bsp\Src\uart.c) 中背靠背请求窗口风险和中断重入边界问题，这是最优先的任务
- `[待完成]` 在开始 `0x06` 前，冻结最小写寄存器规则：可写地址、只读地址、异常码口径、成功回显规则
- `[待完成]` 当前接收链进一步修复后，再正式进入 `0x06` 最小闭环开发，只做纯寄存器写回，不混入外设联动
- `[新增-想法1]` 后续可增加"按键触发一次当前栈水位上报"的维护入口；但该能力不应在当前阶段绕过日志隔离直接污染 Modbus UART，建议放在 `0x06` 最小闭环稳定后，再按"按键触发单次上报 + 低水位告警立即上报 + 恢复后上报一次恢复信息"的思路单独设计与验证

***

## 2026-04-29 | 协议层重构与错误处理修复阶段（Commit: `ed8a4f8` / `44cd3af` / `76d711b`）

### ✅ 完成内容

1. **回归验证与文档整理**

- 完成了完整最小回归测试，覆盖正常帧、越界帧、CRC 错帧、非法功能码帧，三档发送间隔下结果均符合预期
- 对 `问题记录.md` 中的原始问题项进行了验证补记，明确哪些问题已修复、哪些只是部分修复、哪些原文判断并不准确

1. **协议层结构重构**（核心亮点）

- 对 [modbus.c](file://d:\STM32\myproject\ModbusRTU_Gateway\App\Protocol\Src\modbus.c) 做了行为不变的结构重构，将主流程拆分为帧校验、`0x03` 处理、响应构建等独立步骤
- 将异常码中的硬编码数字统一为协议常量，给后续扩展 `0x06` 留出更清晰的入口
- **重构价值**：提高代码可读性和可扩展性

1. **错误处理修复**

- 清理 `error_handler.h/.c` 中遗留的测试接口和测试注释，避免错误处理入口继续分叉
- 统一错误文本发送路径，保证日志输出逻辑更集中，后续继续调整时不需要多处同步修改

1. **监控行为优化**

- 优化栈低水位告警逻辑，改为"进入告警时提示一次，恢复后提示一次"，不再每轮周期性重复刷屏
- 完成结构重构后的回归测试，正常帧、越界帧、CRC 错帧均符合预期，没有引入新的通信回归

### ⚠️ 问题记录

- `问题记录.md` 中"错误处理完全不符合规范""内存管理存在致命风险"等表述存在写过头或不准确的情况，需要按当前真实代码状态继续修正文档结论
- 当前主线能力仍然是"稳定的 Modbus RTU 从机基础版"，协议层只稳定支持 `0x03`，`0x06` 仍属于下一阶段任务
- 本轮完成的是技术债务修复，不是功能扩展；是否合并该分支更适合在明天清醒状态下再做最终确认

### 🎯 后续计划

- `[已完成]` 已完成结构重构后的补充回归验证，正常帧、越界帧、CRC 错帧、非法功能码帧均符合预期
- `[已完成]` 已继续修正 `问题记录.md` 中不准确或已过时的判断，补充了本轮验真与处理状态
- `[待完成]` 检查 `tech-debt/modbus-core-refactor` 分支状态与工作区状态，确认是否合并回 `main`
- `[待完成]` 合并完成后再进入下一阶段，围绕 `0x06` 写单寄存器和外设映射做功能设计，不在当前分支继续堆新功能

***

## 2026-04-28 | 奇偶交替响应问题定位与修复阶段

### ✅ 完成内容

1. **通信故障定位**（核心亮点）

- **定位方法**：对"奇数次响应、偶数次无响应"进行了专项 Debug，采用手动单次发送 + Watch 变量对比的方式，连续记录多次请求结果
- **问题分析**：通过临时调试计数器确认 UART 每次都能完整收到 8 字节请求帧，CRC 也能通过，问题不在协议 CRC 校验本身
- **根本原因**：最终定位到 [BSP\_UART\_ReadFrame()](file://d:\STM32\myproject\ModbusRTU_Gateway\Bsp\Inc\uart.h#L29-L29) 双缓冲切换后，HAL 接收目标没有同步重绑到新缓冲区头部，导致下一帧首字节偶数次稳定错位为 `0x00`

1. **代码修复与清理**

- 在 `Bsp/Src/uart.c` 中新增"读帧后显式 `HAL_UART_AbortReceive()` + 从新缓冲区头重新 `HAL_UART_Receive_IT()`"的修复逻辑，解决确定性的奇偶交替响应故障
- 清理本轮定位问题临时增加的调试变量和测试代码，只保留最终有效修复，保证代码恢复到可维护状态
- 恢复栈高水位监测日志输出，并将监测周期从 `10s` 调整为 `30s`，降低对串口联调的干扰

1. **项目状态复查**

- 完成当前代码、文档、测试分支状态复查，明确现阶段仍是"Modbus RTU 从机一期底座"，尚未进入完整网关阶段
- 梳理出当前项目仍缺写功能码、文档一致性、长期稳定性验证等关键收尾项

### ⚠️ 问题记录

- 本轮最核心问题不是 CRC 顺序错误，而是 UART 双缓冲切换后接收落点未同步重绑，导致偶数次请求首字节错位，协议层被误判为地址不匹配
- `docs` 中仍存在历史测试结论、当前实现、归档记录混杂的问题，部分文档与当前代码配置不完全一致，后续需要继续修复
- 当前从机主流程仍只支持 `0x03`，尚未实现 `0x06`、`0x10` 写功能码，暂时无法承载真正的外设控制闭环

### 🎯 后续计划

- 继续做修复后的回归验证，至少覆盖正常帧、越界帧、错误 CRC 帧、非法功能码帧，确认本轮 UART 接收链修复没有引入新回归
- 开始整理并冻结"当前真实能力边界"，同步更新 `README`、`问题记录.md`、测试记录和工作日志，避免旧文档继续误导后续开发
- 按下一阶段需求优先补从机写路径设计，先明确 `0x06` 写单寄存器的寄存器映射、读写权限和验收路径，为舵机和温湿度接入做接口准备
- 评估并逐步补齐长期稳定性能力，包括更合理的错误恢复路径、看门狗、资源监控项扩展，以及测试分支合并前的最小回归门槛

***

## 2026-04-26 | 健壮性修复阶段（Commit: `cd0134f`）

### ✅ 完成内容

1. **系统防御性校验**

- 增加 `System_ValidateConfig` 配置校验与启动失败回滚，提升系统可预期性
- 将任务启动失败场景集中到统一处理路径，避免系统在半初始化状态下继续运行

1. **UART 接收链治理**

- 完善 `HAL_UART_Receive_IT` 多路径返回值检查，增强异常可见性
- 增加串口错误分级处理与恢复链节流，降低异常场景下的热循环风险
- 优化 [BSP\_UART\_ReadFrame](file://d:\STM32\myproject\ModbusRTU_Gateway\Bsp\Inc\uart.h#L29-L29) 临界区，移除关中断区 `memcpy`，缩短不可抢占窗口

1. **任务生命周期规范化**

- 规范 `LED/UART/MONITOR` 停止流程与超时兜底机制
- 整理任务优雅退出与超时强制终止路径

### ⚠️ 问题记录

- 连续请求下出现"奇数次响应、偶数次无响应"现象，已初步定位为接收链冲突风险点
- 文档历史有过丢失，后续需要持续做模板化和归档管理，避免再次出现记录断裂

### 🎯 后续计划

- 继续定位"奇数次响应、偶数次无响应"问题并修复
- 完成 `0x06` 写单寄存器功能，完善寄存器映射与本期功能收尾
- 继续完善 UART 恢复链的边界情况处理，避免误判与恢复抖动

***

## 2026-04-24 | 分层重构阶段（Commit: `669e932`）

### ✅ 完成内容

1. **应用层结构重构**（核心亮点）

- 完成大规模分层重构：按 `Driver/Protocol/System/Task` 重新组织 App 结构
- 将 [main.c](file://d:\STM32\myproject\ModbusRTU_Gateway\Core\Src\main.c) 简化为初始化 + 启动入口，任务创建集中到 `system_ctrl`
- 设计思路：降低模块间耦合度，提高代码可维护性

1. **跨层边界整理**

- 清理跨层耦合，规范 BSP 与 FreeRTOS 依赖关系
- 整合分散接口，降低主流程的全局暴露程度

### ⚠️ 问题记录

- 重构后暴露出启动失败回滚与任务退出路径健壮性问题
- UART 恢复链在异常场景仍可能触发连锁错误，需要继续治理

### 🎯 后续计划

- 增加启动配置校验 `fail-fast` 机制
- 补充任务优雅退出与统一回滚处理
- 深化 UART 错误分级与恢复逻辑治理

***

## 2026-04-21 | Modbus 功能完成与验证阶段（Commit: `594ba4c`）

### ✅ 完成内容

1. **协议主流程实现**

- 完成协议阶段验证与回归，确认 Modbus RTU 基础功能可持续联调
- 形成 `0x03` 读保持寄存器基础闭环，保证主站工具可读回寄存器数据

1. **联调记录整理**

- 对主站工具联调结果进行归档，形成可复现测试口径
- 对异常响应与回归结果进行了阶段性记录

### ⚠️ 问题记录

- 通信链路在特定场景下仍有抖动，稳定性优化未完全结束
- 文档与代码版本偶发不同步，后续需要统一更新

### 🎯 后续计划

- 进入主干重构，推进分层、去耦、任务生命周期统一管理
- 清理历史遗留接口，减少全局暴露
- 为后续长期稳定性整改预留统一入口

***

## 2026-04-18 | 从机核心流程落地阶段

### ✅ 完成内容

1. **Modbus 从机实现**

- 完成 Modbus RTU 从机核心流程：地址过滤、CRC16 校验、`0x03` 读保持寄存器
- 补充异常响应路径，保证越界和非法访问可返回标准异常帧

1. **阶段联调启动**

- 启动阶段性联调验证，重点关注通信稳定性与数据正确性
- 对正常帧、越界帧、异常响应场景进行了初步联调

### ⚠️ 问题记录

- RS485 链路在连续压力下出现响应不稳，需继续排查接收链时序
- 超时与重试策略仍需优化，避免误判故障
- 发现 RS485 方向控制时序问题，已记录 `issue #15`
- Modbus 响应超时率仍达 `8%`，需进一步排查

### 🎯 后续计划

- 继续压测并记录"正常/异常/无应答"三类统计
- 准备进入代码分层与架构优化阶段
- 修复 RS485 时序问题，优化 Modbus 超时机制

***

## 2026-04-15 | 协议接口预整理阶段

### ✅ 完成内容

1. **接口边界梳理**

- 进入 Modbus RTU 从机实现前的接口预整理，明确 `App/Driver/Protocol/System` 交互边界
- 梳理寄存器访问路径与 CRC 校验入口，准备协议主流程落地

1. **联调准备**

- 增加联调日志，持续验证主机请求到从机响应的闭环
- 为后续协议主流程实现提前准备调试与验证路径

### ⚠️ 问题记录

- 不同模块间配置定义存在分散，维护成本偏高
- 协议链路可读性一般，后续需要进一步结构化

### 🎯 后续计划

- 完成 `0x03` 读保持寄存器稳定实现
- 增补地址过滤、CRC 校验、异常响应基础能力
- 为后续异常帧联调做好验证准备

***

## 2026-04-13 | 工程基础框架搭建阶段（Commit: `30e5dc3`）

### ✅ 完成内容

1. **基础工程搭建**

- 完成工程基础框架落地，包括 FreeRTOS 任务调度、LED/UART 基础驱动和工程目录初步成型
- 打通 `LED 500ms` 闪烁验证路径，建立系统"活性"观测点

1. **基础通信验证**

- 完成 UART `115200bps` 基础通信验证
- 补充项目 README 初版说明，形成最早的项目说明入口

### ️ 问题记录

- 任务边界和模块职责尚未完全明确，后续有重构空间
- 串口链路仅"可跑通"，异常与恢复机制不足

### 🎯 后续计划

- 在现有框架上补充 Modbus 协议处理链路
- 加强串口接收可靠性与可观测日志
- 为后续分层和协议实现打基础

***

